# 任务卡：成员2 第二轮迭代持续集成与集成验收

## 任务目标

建立学生、管理员和Excel三条业务链的集成测试与发布门禁。

## 任务背景

当前147项测试通过，但未登录仍可访问多个占位URL，说明模块测试不能替代业务闭环验收。

## 前置依赖

- 已冻结的`student_profile_contract.md`、`admin_query_contract.md`、`excel_import_contract.md`
- `import_rollback_contract.md`及后续模型评审结论
- 成员4学生个人页候选
- 成员5管理员权限与查询候选
- 成员7导入候选

## 允许范围

- `.github/workflows/ci.yml`
- `scripts/ci_guard.py`
- `tests/`中的跨模块集成测试
- 发布测试报告

## 禁止范围

- 不修改业务规则或实现。
- 不降低检查强度以使PR通过。
- 不使用真实学生Excel文件或生产环境密钥。

## 接口契约

消费全部冻结契约；学生总篇数回退、管理员认证隔离、有效行局部导入、确认原子性和回滚冲突均以契约为准。发现不一致时报告提供方，不在测试中创建兼容逻辑。

## 实施建议

按匿名、学生、viewer_admin、data_admin四种身份建立矩阵；使用动态虚构Excel覆盖上传到查询的完整链路。

## 必须测试

- 学生登录→本人页→退出。
- viewer_admin查询允许、导入403。
- data_admin查询和导入允许。
- 上传→预览零写入→确认→查询→审计。
- `ImportChangeRecord`模型与迁移评审通过后，测试最近一次成功批次回滚、摘要冲突整批拒绝和回滚证据保留。
- Ubuntu/Windows同一SHA。

## 验收标准

- 未授权入口均由后端拒绝。
- 三条业务链的状态、数据和权限断言明确。
- 最终候选没有数据库、上传文件或缓存污染。
