# 第一版收敛阶段总结

## 当前状态

基线：`develop@42abdf7915e1cfe2cb55d889b07648012b360f7d`。

已完成：工程基础、冻结模型、学生认证、学生个人档案、Excel纯解析器、双平台CI。管理员查询与权限、Excel上传预览、正式导入和最近批次回滚仍在开发。

当前状态的唯一入口为`docs/current_project_status.md`，完整作业方案为`docs/sprint/mvp_convergence_governance_plan.md`。

## 当前开发顺序

```text
文档治理
→ 管理员认证、权限、查询、审计
→ 上传、服务端预览、历史、下载
→ 正式事务导入
→ 最近成功批次回滚与SQLite备份
→ 集成验收与文档封板
```

## 第一版回滚结论

不新增数据库快照模型或迁移，不修改思想汇报唯一约束。正式导入前生成服务端JSON业务快照和SQLite安全备份；普通网页回滚只使用JSON快照恢复最近一次成功批次，SQLite备份只用于停机灾难恢复。

## 完成门禁

每项任务必须具备代码、测试、Module Notes、契约状态更新、最终SHA和三项CI成功证据。文档治理不得进入业务实现范围。
