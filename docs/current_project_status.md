# 当前项目状态

## 状态基线

| 项目 | 内容 |
| --- | --- |
| 集成分支 | `develop` |
| 当前候选分支 | `feat/dep-ip-http` |
| 当前阶段 | 任务1：容器CI与固定公网IPv4 HTTP部署候选收敛 |
| 业务规范 | `docs/spec.md` V1.4 |
| 部署入口 | 固定公网IPv4 + HTTP + Docker Compose单机部署 |

本文件区分代码已实现、本地已验证、CI已验证和服务器已验证；四者不得互相替代。

## 总体结论

核心业务MVP、Excel导入与业务回滚、SQLite灾难恢复、Docker镜像、生产Compose、静态资源、健康检查和COS备份代码已经形成。当前正在完成容器CI和HTTP部署候选收敛，尚未部署到WorkBuddy或正式服务器，不得描述为已经上线。

## 部署任务状态

| 任务 | 状态 | 剩余门禁 |
| --- | --- | --- |
| 任务1：发布候选收敛 | 开发中 | 容器CI全绿、固定候选SHA、合并评审 |
| 任务2：WorkBuddy服务器演练 | 未开始 | 主机权限与规格确认、合成数据部署、重启、回退和恢复演练 |
| 任务3：自购服务器正式部署 | 等待服务器 | 资产登记、安全组、生产备份、监控、审批和正式版本Tag |

## 任务1当前证据

- Ubuntu、Windows、Repository policy和容器合同检查已经通过。
- 远端镜像构建和Trivy扫描已经通过。
- 容器冒烟曾因 `PARTY_ARCHIVE_ROOT` 多拼接一层 `data` 导致绑定挂载失败；修复后由新提交重新验证。
- 当前部署合同只发布Nginx 80端口，Web 8000、443和数据库端口不发布。
- HTTP入口必须由云安全组、宿主机防火墙、校园网或VPN限制可信来源；不可直接向不受信任公网开放。

## 服务器前后阶段

拿到自购服务器前，先完成任务1并在WorkBuddy服务器使用合成数据执行任务2。拿到自购服务器后，复用同一固定候选镜像和Compose结构执行任务3，但必须使用全新秘密、管理员账号、数据库和备份目标。

真实IP、联系人、密钥、`.env`、SQLite、Excel、媒体和备份不得提交仓库。
