# Sprint 2 契约冻结决策记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 决策日期 | 2026-08-09 |
| 依据基线 | `develop@068c27fb7e41ec6f77299ebf3bbac68162714f63` |
| 决策范围 | 学生个人页、管理员查询、Excel导入、导入回滚 |
| 原则 | 冻结跨模块可观察行为，不冻结实现代码文件 |

## 2. 已确认决策

| 编号 | 决策 | 冻结结论 |
| --- | --- | --- |
| D1 | 学生最近更新时间 | 取学生、申请记录、汇总和有效思想汇报实际时间最大值 |
| D2 | 管理员登录失败审计 | 不写业务`OperationLog` |
| D3 | 非法查询筛选 | 返回200并显示错误，结果为空，不返回全量数据 |
| D4 | 管理员列表分页 | 固定每页20条，不开放`page_size` |
| D5 | 详情404审计 | 不记录成功查看事件 |
| D6 | Excel最大上传尺寸 | 10 MiB |
| D7 | 预览保存 | 保存文件、批次、错误和警告；确认时校验哈希并重新解析 |
| D8 | 确认原子性 | 整批成功或整批失败，禁止部分成功 |
| D9 | 空值语义 | 普通可选标量默认不覆盖；思想汇报集合按有效行完整替换 |
| D10 | 幂等标识 | `ImportBatch.id` |
| D11 | 回滚范围 | 仅最新成功、未回滚且无后续变化的批次 |
| D12 | 回滚证据 | 新增结构化`ImportChangeRecord`模型与迁移，实施前单独评审 |
| D13 | 回滚冲突 | 摘要不一致即整批拒绝，不自动合并、不强制覆盖 |
| D14 | 认证隔离 | 管理员退出保留`student_id`；学生退出保留Django管理员认证 |
| D15 | 总篇数展示 | 遵循V1.2：填报值非空时使用填报值，否则回退计算数并标注来源 |

## 3. 上位规范冲突处理

旧成员规格与任务卡中的“`reported_total_count=None`显示未知”不符合`docs/spec.md` V1.2第四章。V1.2属于MVP需求冻结规范，因此正式契约采用D15。成员4和成员2必须以`student_profile_contract.md`为实现与测试依据。

## 4. 实现解锁

| 成员 | 已解锁范围 | 仍受限范围 |
| --- | --- | --- |
| 成员4 | 学生个人页只读展示与测试 | 不修改认证、模型或迁移 |
| 成员5 | 管理员权限、登录、查询、详情和审计服务 | 不进入Excel业务，不修改学生认证 |
| 成员7 | 上传、预览、确认、历史和文件访问 | 回滚须先通过变更记录模型与迁移评审 |

任何实现偏离本记录时，必须先更新对应契约和测试并完成提供方、消费者及成员1复审。
