# 党建档案系统部署准备报告（阶段 0–3，暂不含 HTTP 入口整改）

**执行日期：** 2026-09-05  
**实现提交：** `e92c7a4ded2d06049bdb95221889361fa2b7382f`  
**工作分支：** `feat/dep-ip-http`  
**结论：** 非 HTTP 范围的代码、配置、测试和文档准备已完成本地验证；尚未合入主干、未取得 GitHub Container CI 结果、未创建 RC tag，因此当前不是正式上线版本。

## 1. 已完成工作

### CI 与发布门禁

- `container-ci.yml` 增加 Django 配置检查、迁移漂移检查及完整测试套件。
- 保留容器契约、镜像构建、Trivy 严重漏洞扫描和 Compose 冒烟门禁。
- 新增压测证据目录忽略规则，避免数据库、合成 Excel 和运行证据进入 Git。
- 新增 `docs/cloud_assets.md` 资产登记模板；真实资产值留待阶段 4 填写。

### AQ-M004 与 PII 防泄漏

- 审计覆盖管理员登录/退出、学生登录成功、学生详情查看、Excel 预览/确认/回滚、后台档案增删改和管理员角色变更。
- 审计仅使用动作码、模型类型和数据库内部 ID 定位对象。
- 删除姓名、学号、管理员显示名、邮箱和原始文件名等动态描述。
- 受保护档案不再写入包含模型 `__str__` 的 Django Admin `LogEntry`，避免姓名和学号旁路落盘。
- `OperationLog` 管理后台设为只读，禁止人工伪造、修改或删除审计记录。

### Gunicorn 与 SQLite

- Gunicorn worker 由固定 1 个改为可配置的 2–3 个，默认 2 个；保留 `gthread` 和默认 2 threads。
- 新增 `GUNICORN_WORKERS=2` 和 `SQLITE_BUSY_TIMEOUT_MS=30000` 生产配置示例。
- 生产 SQLite 新连接执行：
  - `PRAGMA journal_mode=WAL`；
  - `PRAGMA busy_timeout=30000`；
  - `PRAGMA synchronous=NORMAL`。
- Django SQLite `OPTIONS.timeout` 与 busy timeout 使用同一环境变量，避免两套超时不一致。

### 合成数据压测

- 新增 `scripts/run_deployment_load_test.py`，拒绝少于 500 条的数据集。
- 压测组合执行批量导入、查询、第二次覆盖导入、回滚、重启持久性检查及近同时确认/回滚并发测试。
- 新增 `docs/deployment_load_test_guide.md`。

## 2. 验证记录

### Django 与完整测试

```text
python manage.py check
System check identified no issues (0 silenced).

python manage.py makemigrations --check
No changes detected

python manage.py test --verbosity 1
Found 412 test(s).
Ran 412 tests in 439.195s
OK
```

最终审计修正后的聚焦回归：

```text
Found 23 test(s).
Ran 23 tests in 9.568s
OK
[PASS] repository policy
```

### 1500 条正式规模合成验收

命令：

```text
python scripts/run_deployment_load_test.py \
  --output-dir artifacts/deployment-load/run-1500-e92c7a4 \
  --student-count 1500 \
  --seed 20260822
```

结果：

```text
git_sha=e92c7a4ded2d06049bdb95221889361fa2b7382f
student_count=1500
acceptance_total_seconds=143.333
concurrency_test_returncode=0
database_locked_errors=0
http_5xx_errors=0
result=passed
restart_verified=true
```

第一次导入创建 1500 名学生、1500 条申请、1500 条汇总和 4500 条思想汇报；第二次导入更新 1500 名学生；回滚后数量与首批一致。负向数据分别按约定返回 400 或 409，没有部分写入。

### SQLite 生产参数实测

在独立合成数据库上以生产模式建立连接后读取：

```text
journal_mode=wal
busy_timeout=30000
synchronous=1
```

其中 SQLite 返回的 `synchronous=1` 对应 `NORMAL`。

## 3. 已核实的现状差异

- 部署分支实际包含约 400 项测试，并非只有约 26 项；本次完整发现数为 412。
- `release/predeploy-closeout` 已是部署分支祖先，业务收尾不需要二次合并。
- AQ-M004 原先已有部分动作，但动态描述包含姓名、学号、管理员显示名或文件名；本次完成去敏和覆盖补齐。
- 原部署配置确为单 worker 且没有生产 WAL 初始化，本次已补齐。

## 4. 遗留风险与未完成项

- 按负责人指令，本轮未修改 HTTP、HTTPS、隧道、Nginx 入口绑定、Host、CSRF 或 TLS 相关配置与文档；现有固定公网 IPv4 + HTTP 基线仍在仓库中。
- 本地没有代替 GitHub Actions 宣称 `container-ci` 已全绿。必须在 PR 的最终提交上取得远端 CI 成功结果。
- 本轮未执行容器 HTTP 冒烟测试，以遵守“暂时跳过 HTTP 相关”的范围限制。
- `docs/cloud_assets.md` 仍是模板，实例 ID、地域和责任人等必须在阶段 4 填写。
- 实现提交尚未合入 `develop`/`main`，没有 RC tag；压测通过不等于批准上线。
- SQLite 仍是单写者。WAL 和 busy timeout 能降低读写冲突，但不能提供多数据库写者的横向扩展能力；生产只允许运行一个 Web 容器。
- 1500 条合成验收的回滚耗时较长，应在维护窗口执行正式导入或恢复，并避免重复提交。

## 5. 合入与 RC 冻结清单

1. 将 `e92c7a4` 通过 PR 合入 `develop`，解决冲突后运行完整 CI。
2. 通过发布 PR 合入 `main`，禁止仅 cherry-pick 部分审计或数据库配置。
3. 在最终 `main` SHA 上重新运行 1500 条压测与容器 CI。
4. 确认 GitHub Container CI 的完整测试、构建、扫描和冒烟任务全部成功。
5. 填写本报告中的最终主干 SHA 和 CI 链接；经评审后创建 annotated tag `rc/dep-c-v1`。
6. RC 创建后禁止移动标签；后续修改必须生成新的 RC。

## 6. 阶段 4 执行清单

1. 先解决并评审暂缓的 HTTP/隧道入口决策，不得沿用冲突配置直接承载真实数据。
2. 填写并双人复核云资产登记表，但不得把秘密值写入 Git。
3. 使用 RC SHA 对应的不可变镜像摘要，不使用浮动 `latest`。
4. 创建 SQLite、媒体、静态和备份持久化目录并核验权限。
5. 启动后验证容器状态、九个党支部、WAL、busy timeout 和 Gunicorn worker 数。
6. 先用合成数据完成登录、查询、导入、详情、下载和回滚演练。
7. 导入真实 Excel 前停止其他写操作并创建一致性备份。
8. 正式导入后核对学生、申请、汇总、思想汇报、错误、警告和审计数量。
9. 检查应用、Nginx、容器和审计日志没有姓名、学号、邮箱、Cookie、Session 或 Excel 正文。
10. 导入后再次备份并验证可读性，由执行人和复核人共同签署 Go/No-Go。

应用回滚只切换到上一已验证镜像摘要并保留数据目录；数据回滚必须停止 Web 写入、验证备份后恢复，禁止在线替换 SQLite 文件。
